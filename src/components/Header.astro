---
const adultiMenu = {
  image: '/wp-content/uploads/2024/08/IMG_2065-scaled.jpg',
  imageAlt: 'Gym Floor Athlon Club',
  columns: [
    {
      heading: 'Palestra',
      links: [
        { label: 'Gym Floor (Sala Pesi)', href: '/gym-floor' },
        { label: 'Personal Training', href: '/personal-training' },
      ],
    },
    {
      heading: 'Corsi Fitness',
      links: [
        { label: 'Tutti i Corsi Fitness', href: '/corsi-fitness' },
        { label: 'Yoga', href: '/yoga' },
        { label: 'Pilates', href: '/pilates' },
        { label: 'Group Reformer', href: '/reformer' },
        { label: 'Calisthenics', href: '/calisthenics' },
        { label: 'Booty Workout', href: '/booty-workout' },
        { label: 'HBX', href: '/hbx' },
        { label: 'Motr®', href: '/motr' },
        { label: 'Body Sculpt', href: '/body-sculpt' },
        { label: 'Body Pump®', href: '/body-pump' },
        { label: 'Antigravity®', href: '/antigravity' },
        { label: 'GP Coreo', href: '/gpcoreo' },
        { label: 'Ginnastica Posturale', href: '/ginnastica-posturale' },
        { label: 'Ginnastica Dolce', href: '/ginnastica-dolce' },
        { label: 'Balli di Gruppo', href: '/balli-di-gruppo' },
        { label: 'Difesa Personale', href: '/difesa-personale' },
        { label: 'Strenght', href: '/strenght' },
      ],
    },
    {
      heading: 'Piscina',
      links: [
        { label: 'Scuola Nuoto Adulti', href: '/scuola-nuoto-adulti' },
        { label: 'Aqua Fitness', href: '/aqua-fitness' },
        { label: 'Nuoto Libero', href: '/nuoto-libero' },
        { label: 'Corso Gestanti', href: '/gestanti' },
      ],
    },
  ],
};

const juniorMenu = {
  image: '/wp-content/uploads/2023/11/Baby-Nuoto.jpg',
  imageAlt: 'Baby Nuoto Athlon Club',
  columns: [
    {
      heading: 'Corsi Junior',
      links: [
        { label: 'Baby Nuoto', href: '/baby-nuoto' },
        { label: 'Scuola Nuoto Bambini', href: '/scuola-nuoto-bambini-3' },
        { label: 'Nuoto Agonistico', href: '/nuoto-agonistico' },
        { label: 'Pallanuoto', href: '/pallanuoto' },
      ],
    },
  ],
};

const adultiCtas = [
  { label: 'Scopri gli Abbonamenti', href: '/abbonamenti', accent: true, arrow: true },
  { label: 'Athlon Club', href: 'https://onelink.to/athlon', sub: "Scarica l'app →" },
  { label: 'Orari Corsi', href: 'https://planning.athlonroma.it/' },
];

const juniorCtas = [
  { label: 'Accedi al tuo account', href: 'https://athlon.perfectgym.com/ClientPortal2/#/Login', accent: true, arrow: true },
  { label: 'Athlon Club', href: 'https://onelink.to/athlon', sub: "Scarica l'app →" },
  { label: 'Scopri gli orari dei corsi', href: 'https://planning.athlonroma.it/' },
];

const menus = [
  { id: 'adulti', label: 'Attività Adulti', data: adultiMenu, ctas: adultiCtas },
  { id: 'junior', label: 'Corsi Junior', data: juniorMenu, ctas: juniorCtas },
];

// Official brand glyphs (Simple Icons paths), drawn in currentColor.
const social = [
  {
    label: 'Instagram',
    href: 'https://www.instagram.com/athlon_club/',
    icon: `<svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor" aria-hidden="true"><path d="M12 2.16c3.2 0 3.58.01 4.85.07 1.17.05 1.8.25 2.23.41.56.22.96.48 1.38.9.42.42.68.82.9 1.38.16.42.36 1.06.41 2.23.06 1.27.07 1.65.07 4.85s-.01 3.58-.07 4.85c-.05 1.17-.25 1.8-.41 2.23-.22.56-.48.96-.9 1.38-.42.42-.82.68-1.38.9-.42.16-1.06.36-2.23.41-1.27.06-1.65.07-4.85.07s-3.58-.01-4.85-.07c-1.17-.05-1.8-.25-2.23-.41-.56-.22-.96-.48-1.38-.9-.42-.42-.68-.82-.9-1.38-.16-.42-.36-1.06-.41-2.23C2.17 15.58 2.16 15.2 2.16 12s.01-3.58.07-4.85c.05-1.17.25-1.8.41-2.23.22-.56.48-.96.9-1.38.42-.42.82-.68 1.38-.9.42-.16 1.06-.36 2.23-.41C8.42 2.17 8.8 2.16 12 2.16M12 0C8.74 0 8.33.01 7.05.07 5.78.13 4.9.33 4.14.63c-.79.3-1.46.72-2.12 1.39C1.35 2.68.93 3.35.63 4.14.33 4.9.13 5.78.07 7.05.01 8.33 0 8.74 0 12s.01 3.67.07 4.95c.06 1.27.26 2.15.56 2.91.3.79.72 1.46 1.39 2.12.66.67 1.33 1.09 2.12 1.39.76.3 1.64.5 2.91.56C8.33 23.99 8.74 24 12 24s3.67-.01 4.95-.07c1.27-.06 2.15-.26 2.91-.56.79-.3 1.46-.72 2.12-1.39.67-.66 1.09-1.33 1.39-2.12.3-.76.5-1.64.56-2.91.06-1.28.07-1.69.07-4.95s-.01-3.67-.07-4.95c-.06-1.27-.26-2.15-.56-2.91-.3-.79-.72-1.46-1.39-2.12C21.32 1.35 20.65.93 19.86.63 19.1.33 18.22.13 16.95.07 15.67.01 15.26 0 12 0Zm0 5.84a6.16 6.16 0 1 0 0 12.32 6.16 6.16 0 0 0 0-12.32ZM12 16a4 4 0 1 1 0-8 4 4 0 0 1 0 8Zm7.85-10.4a1.44 1.44 0 1 1-2.88 0 1.44 1.44 0 0 1 2.88 0Z"/></svg>`,
  },
  {
    label: 'Facebook',
    href: 'https://www.facebook.com/AthlonClubRoma',
    icon: `<svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor" aria-hidden="true"><path d="M24 12.07C24 5.4 18.63 0 12 0S0 5.4 0 12.07C0 18.1 4.39 23.1 10.13 24v-8.44H7.08v-3.49h3.05V9.41c0-3.02 1.79-4.69 4.53-4.69 1.31 0 2.68.24 2.68.24v2.97h-1.51c-1.49 0-1.96.93-1.96 1.89v2.25h3.33l-.53 3.49h-2.8V24C19.61 23.1 24 18.1 24 12.07Z"/></svg>`,
  },
  {
    label: 'Youtube',
    href: 'https://www.youtube.com/channel/UC9XX5E0u-qqlqavtawlmOnQ',
    icon: `<svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor" aria-hidden="true"><path d="M23.5 6.19a3.02 3.02 0 0 0-2.12-2.14C19.5 3.55 12 3.55 12 3.55s-7.5 0-9.38.5A3.02 3.02 0 0 0 .5 6.19C0 8.08 0 12 0 12s0 3.92.5 5.81a3.02 3.02 0 0 0 2.12 2.14c1.88.5 9.38.5 9.38.5s7.5 0 9.38-.5a3.02 3.02 0 0 0 2.12-2.14C24 15.92 24 12 24 12s0-3.92-.5-5.81ZM9.55 15.57V8.43L15.82 12l-6.27 3.57Z"/></svg>`,
  },
];
---

<header class="site-header" id="site-header">
  <div class="container header-inner">
    <a href="/" class="logo">
      <img src="/wp-content/uploads/2025/08/Pittogramma.png" alt="Athlon Club" width="32" height="32" />
    </a>

    <a href="/contatti" class="header-contact-mobile">Contattaci</a>

    <nav class="main-nav">
      {menus.map((m) => (
        <div class="nav-item" data-nav-item={m.id}>
          <button class="nav-trigger" data-nav-trigger={m.id} aria-expanded="false">
            {m.label}
            <span class="chev" aria-hidden="true"></span>
          </button>
        </div>
      ))}
      <a href="/contatti" class="nav-cta">Contattaci</a>
    </nav>

    <div class="header-actions">
      <a href="https://athlon.perfectgym.com/ClientPortal2/#/Login" class="header-link">Accedi</a>
      <a href="https://athlon.perfectgym.com/ClientPortal2/#/Registration" class="header-link strong">Iscriviti</a>
      <button class="burger" aria-label="Menu" aria-expanded="false" id="burger">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>

  <!-- Desktop / tablet mega menu panels -->
  <div class="mega-backdrop" data-mega-backdrop></div>
  {menus.map((m) => (
    <div class="mega" id={`mega-${m.id}`} data-mega={m.id}>
      <div class="container mega-inner">
        <div class="mega-image">
          <img src={m.data.image} alt={m.data.imageAlt} loading="lazy" />
        </div>
        <div class="mega-columns" style={`--col-count:${m.data.columns.length}`}>
          {m.data.columns.map((col) => (
            <div class="mega-col">
              <span class="mega-col-heading">{col.heading}</span>
              <ul>
                {col.links.map((l) => (
                  <li><a href={l.href}>{l.label}</a></li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>

      <div class="mega-cta">
        {m.ctas.map((c) => (
          <a
            href={c.href}
            class:list={['mega-cta-item', { 'mega-cta-item--accent': c.accent }]}
          >
            {c.label}
            {c.sub && <span class="mega-cta-sub">{c.sub}</span>}
            {c.arrow && <span aria-hidden="true">↓</span>}
          </a>
        ))}
      </div>

      <div class="mega-social">
        <div class="container mega-social-inner">
          <div class="mega-social-icons">
            {social.map((s) => (
              <a href={s.href} target="_blank" rel="noopener" aria-label={s.label} set:html={s.icon} />
            ))}
          </div>
        </div>
      </div>
    </div>
  ))}

  <!-- Mobile / tablet full-screen menu -->
  <div class="mobile-panel" id="mobile-panel">
    <span class="mobile-eyebrow">Menu</span>

    <ul class="mobile-list">
      {menus.map((m) => (
        <li class="mobile-group">
          <button class="mobile-link mobile-group-trigger" aria-expanded="false">
            {m.label}
            <span class="mobile-chev" aria-hidden="true"></span>
          </button>
          <div class="mobile-submenu">
            {m.data.columns.length === 1 ? (
              /* A single group (Junior) would mean tapping twice to reach the
                 same four links — show them straight away instead. */
              <ul class="mobile-sublist mobile-sublist--flat">
                {m.data.columns[0].links.map((l) => (
                  <li><a class="mobile-sublink" href={l.href}>{l.label}</a></li>
                ))}
              </ul>
            ) : (
              m.data.columns.map((col) => (
                <div class="mobile-subgroup">
                  <button class="mobile-sublabel mobile-subgroup-trigger" aria-expanded="false">
                    {col.heading}
                    <span class="mobile-chev small" aria-hidden="true"></span>
                  </button>
                  <ul class="mobile-sublist">
                    {col.links.map((l) => (
                      <li><a class="mobile-sublink" href={l.href}>{l.label}</a></li>
                    ))}
                  </ul>
                </div>
              ))
            )}
          </div>
        </li>
      ))}
    </ul>

    <div class="mobile-secondary">
      <a class="mobile-secondary-btn" href="https://onelink.to/athlon">Scarica l'app</a>
      <a class="mobile-secondary-btn" href="https://planning.athlonroma.it/">Orari Corsi</a>
      <a class="mobile-secondary-btn" href="https://athlon.perfectgym.com/ClientPortal2/#/Login">Accedi</a>
    </div>

    <div class="mobile-ctas">
      <a class="btn btn-primary mobile-cta" href="/abbonamenti">Scopri gli Abbonamenti</a>
      <a class="btn btn-outline-dark mobile-cta" href="https://athlon.perfectgym.com/ClientPortal2/#/Registration">Iscriviti</a>
    </div>
  </div>
</header>

<script>
  const header = document.getElementById('site-header');
  const burger = document.getElementById('burger');
  const panel = document.getElementById('mobile-panel');
  const backdrop = document.querySelector('[data-mega-backdrop]') as HTMLElement | null;
  const navItems = Array.from(document.querySelectorAll<HTMLElement>('[data-nav-item]'));
  const megaPanels = Array.from(document.querySelectorAll<HTMLElement>('[data-mega]'));
  const canHover = window.matchMedia('(hover: hover) and (pointer: fine)').matches;

  function closeAllMega() {
    navItems.forEach((item) => {
      item.classList.remove('open');
      item.querySelector('[data-nav-trigger]')?.setAttribute('aria-expanded', 'false');
    });
    megaPanels.forEach((mega) => mega.classList.remove('is-visible'));
    backdrop?.classList.remove('visible');
  }

  function openMega(id: string) {
    navItems.forEach((item) => {
      const isTarget = item.dataset.navItem === id;
      item.classList.toggle('open', isTarget);
      item.querySelector('[data-nav-trigger]')?.setAttribute('aria-expanded', String(isTarget));
    });
    megaPanels.forEach((mega) => mega.classList.toggle('is-visible', mega.dataset.mega === id));
    backdrop?.classList.add('visible');
  }

  let closeTimer: number | undefined;
  const cancelClose = () => window.clearTimeout(closeTimer);
  const scheduleClose = () => {
    cancelClose();
    closeTimer = window.setTimeout(closeAllMega, 320);
  };

  navItems.forEach((item) => {
    const id = item.dataset.navItem!;
    const trigger = item.querySelector<HTMLButtonElement>('[data-nav-trigger]');

    trigger?.addEventListener('click', () => {
      // On hover devices the panel is already open by the time a click lands,
      // so toggling here would read as "it closes when I click it".
      if (canHover) {
        cancelClose();
        openMega(id);
        return;
      }
      item.classList.contains('open') ? closeAllMega() : openMega(id);
    });

    if (canHover) {
      // Only ever opens/switches. Closing is owned by the header below, so
      // drifting off the trigger into the dead space beside or under it
      // doesn't dismiss the panel.
      item.addEventListener('mouseenter', () => {
        cancelClose();
        openMega(id);
      });
    }
  });

  // The panels are descendants of <header>, so mouseleave on the header only
  // fires once the pointer has left the bar *and* the open panel — the gap
  // between them, and any wobble along the way, counts as "still inside".
  // That is what stops the menu from snapping shut on a near-miss.
  if (canHover) {
    header?.addEventListener('mouseleave', scheduleClose);
    header?.addEventListener('mouseenter', cancelClose);
    megaPanels.forEach((mega) => mega.addEventListener('mouseenter', cancelClose));
  }

  backdrop?.addEventListener('click', closeAllMega);
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeAllMega();
  });

  // Mobile burger — full-screen overlay, so lock the page behind it.
  function closeMobileMenu() {
    header?.classList.remove('menu-open');
    burger?.setAttribute('aria-expanded', 'false');
    document.body.classList.remove('nav-mobile-locked');
  }

  burger?.addEventListener('click', () => {
    const isOpen = header?.classList.toggle('menu-open');
    burger.setAttribute('aria-expanded', String(!!isOpen));
    document.body.classList.toggle('nav-mobile-locked', !!isOpen);
  });

  panel?.querySelectorAll('a').forEach((a) => a.addEventListener('click', closeMobileMenu));

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeMobileMenu();
  });

  // Accordion: only one group open at a time.
  const mobileGroups = Array.from(document.querySelectorAll<HTMLElement>('.mobile-group'));
  mobileGroups.forEach((group) => {
    const trigger = group.querySelector<HTMLButtonElement>('.mobile-group-trigger');
    trigger?.addEventListener('click', () => {
      const willOpen = !group.classList.contains('open');
      mobileGroups.forEach((other) => {
        other.classList.remove('open');
        other.querySelector('.mobile-group-trigger')?.setAttribute('aria-expanded', 'false');
      });
      if (willOpen) {
        group.classList.add('open');
        trigger.setAttribute('aria-expanded', 'true');
      }
    });
  });

  // Second level (Palestra / Corsi Fitness / Piscina) — also one at a time,
  // but scoped to its own parent group.
  document.querySelectorAll<HTMLElement>('.mobile-submenu').forEach((submenu) => {
    const subgroups = Array.from(submenu.querySelectorAll<HTMLElement>('.mobile-subgroup'));
    subgroups.forEach((subgroup) => {
      const trigger = subgroup.querySelector<HTMLButtonElement>('.mobile-subgroup-trigger');
      trigger?.addEventListener('click', () => {
        const willOpen = !subgroup.classList.contains('open');
        subgroups.forEach((other) => {
          other.classList.remove('open');
          other.querySelector('.mobile-subgroup-trigger')?.setAttribute('aria-expanded', 'false');
        });
        if (willOpen) {
          subgroup.classList.add('open');
          trigger.setAttribute('aria-expanded', 'true');
        }
      });
    });
  });
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(255, 255, 255, 0.72);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border-bottom: 1px solid rgba(39, 36, 35, 0.08);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 10px;
    padding-bottom: 10px;
    gap: 24px;
  }

  .logo img {
    display: block;
    transition: transform var(--dur-fast) var(--ease-out-crit);
  }

  .logo:active img {
    transform: scale(0.92);
  }

  /* Mobile-only pill in the bar — desktop keeps the CTA inside the nav. */
  .header-contact-mobile {
    display: none;
  }

  .main-nav {
    display: flex;
    align-items: center;
    gap: 8px;
    flex: 1;
    justify-content: center;
  }

  .nav-item {
    position: relative;
  }

  .nav-trigger {
    display: flex;
    align-items: center;
    gap: 6px;
    background: none;
    border: none;
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    color: var(--color-dark);
    cursor: pointer;
    padding: 8px 14px;
    border-radius: 100px;
    transition: background-color var(--dur-fast) var(--ease-out-crit);
  }

  .nav-item.open .nav-trigger,
  .nav-trigger:hover {
    background: rgba(39, 36, 35, 0.06);
  }

  .chev {
    width: 8px;
    height: 8px;
    border-right: 1.5px solid currentColor;
    border-bottom: 1.5px solid currentColor;
    transform: rotate(45deg) translateY(-2px);
    transition: transform var(--dur-fast) var(--ease-out-crit);
  }

  .nav-item.open .chev {
    transform: rotate(-135deg) translateY(0px);
  }

  .nav-cta {
    margin-left: 8px;
    padding: 9px 20px;
    border-radius: 100px;
    background: var(--color-dark);
    color: var(--color-white);
    font-size: 13px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .nav-cta:hover {
    background: var(--color-primary);
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 18px;
  }

  .header-link {
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
  }

  .header-link.strong {
    color: var(--color-primary);
  }

  /* ---- Mega menu ---- */
  .mega-backdrop {
    position: fixed;
    inset: 0;
    top: 0;
    background: rgba(10, 10, 8, 0.4);
    opacity: 0;
    pointer-events: none;
    transition: opacity var(--dur-base) ease;
    z-index: 89;
  }

  .mega-backdrop.visible {
    opacity: 1;
    pointer-events: auto;
  }

  .mega {
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    z-index: 90;
    background: var(--color-dark);
    color: var(--color-white);
    opacity: 0;
    transform: translateY(-8px);
    pointer-events: none;
    transition: opacity var(--dur-base) var(--ease-out-crit), transform var(--dur-base) var(--ease-out-crit);
    box-shadow: 0 30px 60px rgba(0, 0, 0, 0.3);
  }

  .mega.is-visible {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  .mega-inner {
    display: flex;
    gap: 48px;
    padding: 40px 24px;
  }

  .mega-image {
    flex: 0 0 34%;
    max-width: 420px;
    border-radius: 16px;
    overflow: hidden;
    aspect-ratio: 4 / 3;
  }

  .mega-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .mega-columns {
    flex: 1;
    display: grid;
    grid-template-columns: repeat(var(--col-count, 3), 1fr);
    gap: 32px;
  }

  .mega-col-heading {
    display: block;
    font-family: var(--font-heading);
    text-transform: uppercase;
    letter-spacing: -0.005em;
    color: var(--color-primary);
    font-size: var(--text-lg);
    margin-bottom: 16px;
  }

  .mega-col ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .mega-col a {
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.85);
    transition: color var(--dur-fast) ease, padding-left var(--dur-fast) var(--ease-out-crit);
  }

  .mega-col a:hover {
    color: var(--color-white);
    padding-left: 4px;
  }

  .mega-cta {
    display: flex;
    border-top: 1px solid var(--border-dark);
  }

  .mega-cta-item {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 24px;
    font-family: var(--font-heading);
    text-transform: uppercase;
    letter-spacing: -0.005em;
    /* A step above the column headings: this bar is the panel's call to
       action, so it should be the loudest text in it rather than the quietest. */
    font-size: var(--text-xl);
    color: var(--color-white);
    background: #35312f;
    border-left: 1px solid var(--border-dark);
    transition: background-color var(--dur-fast) ease;
  }

  .mega-cta-item:first-child {
    border-left: none;
  }

  .mega-cta-item--accent {
    background: var(--color-primary);
  }

  .mega-cta-item:hover {
    background: #443e3b;
  }

  .mega-cta-item--accent:hover {
    background: var(--accent-hover);
  }

  .mega-cta-sub {
    font-family: var(--font-body);
    text-transform: none;
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.75);
    letter-spacing: 0;
  }

  .mega-social {
    background: var(--color-cream);
  }

  .mega-social-inner {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 16px 24px;
  }

  .mega-social-icons {
    display: flex;
    justify-content: center;
    gap: 12px;
  }

  .mega-social-icons a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--color-dark);
    color: var(--color-white);
    transition: background-color var(--dur-fast) ease, transform var(--dur-fast) var(--ease-out-crit);
  }

  .mega-social-icons a:hover {
    background: var(--color-primary);
    transform: translateY(-2px);
  }

  .mega-social-icons :global(svg) {
    display: block;
  }

  .burger {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 32px;
    height: 32px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
  }

  .burger span {
    width: 24px;
    height: 2px;
    background: var(--color-dark);
    transition: transform var(--dur-fast) var(--ease-out-crit), opacity var(--dur-fast) var(--ease-out-crit);
    transform-origin: center;
  }

  .menu-open .burger span:nth-child(1) {
    transform: translateY(7px) rotate(45deg);
  }
  .menu-open .burger span:nth-child(2) {
    opacity: 0;
  }
  .menu-open .burger span:nth-child(3) {
    transform: translateY(-7px) rotate(-45deg);
  }

  .mobile-panel {
    display: none;
  }

  @media (max-width: 1100px) {
    .mega-columns {
      grid-template-columns: repeat(min(var(--col-count, 3), 2), 1fr);
    }
  }

  /* Width alone is the wrong test: a 27" portrait kiosk is ~1080px wide but
     driven by a finger, so it was getting the hover-only desktop menu. Any
     coarse pointer gets the touch menu regardless of how wide the screen is. */
  @media (max-width: 960px), (pointer: coarse) {
    /* backdrop-filter on an ancestor makes it the containing block for
       position:fixed descendants — with it the overlay below would size
       itself to the header instead of the viewport, which is what pushed the
       panel off to the right on the kiosk. Solid bar here. */
    .site-header {
      background: var(--color-white);
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
    }

    .main-nav,
    .header-link,
    .mega,
    .mega-backdrop {
      display: none !important;
    }

    /* Mirrors the live site's mobile bar, trimmed a little shorter so it
       eats less of the viewport when sticky. */
    .site-header {
      border-bottom: 1px solid var(--color-primary);
    }

    .header-inner {
      height: 72px;
      padding: 0 24px 0 16px;
      gap: 12px;
    }

    .logo img {
      width: 52px;
      height: auto;
    }

    .header-contact-mobile {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto;
      padding: 10px 14px;
      border: 1px solid var(--color-primary);
      border-radius: 20px;
      background: var(--color-white);
      color: var(--color-primary);
      font-size: 12px;
      font-weight: 500;
      letter-spacing: 1.25px;
      line-height: 1;
      text-transform: uppercase;
      white-space: nowrap;
    }

    .header-actions {
      gap: 0;
    }

    .burger {
      display: flex;
      position: relative;
      z-index: 2;
      width: 28px;
      height: 28px;
      gap: 7px;
      padding: 0;
    }

    .burger span {
      width: 28px;
    }

    /* The live site uses a two-rule icon, not three. */
    .burger span:nth-child(2) {
      display: none;
    }

    .menu-open .burger span:nth-child(1) {
      transform: translateY(4.5px) rotate(45deg);
    }

    .menu-open .burger span:nth-child(3) {
      transform: translateY(-4.5px) rotate(-45deg);
    }

    /* Logo and pill sit above the overlay so the bar stays intact while the
       menu is open. */
    .logo,
    .header-contact-mobile {
      position: relative;
      z-index: 2;
    }

    /* Full-screen translucent overlay rather than an inline accordion that
       pushes the page down — keeps the menu short and the page still. */
    .mobile-panel {
      position: fixed;
      inset: 0;
      z-index: 1;
      display: flex;
      flex-direction: column;
      /* Clears the 72px bar so the burger stays reachable. */
      padding: 92px 1.5rem 2rem;
      background: rgba(255, 255, 255, 0.94);
      backdrop-filter: blur(24px) saturate(160%);
      -webkit-backdrop-filter: blur(24px) saturate(160%);
      opacity: 0;
      pointer-events: none;
      transition: opacity var(--dur-base) ease;
    }

    .menu-open .mobile-panel {
      opacity: 1;
      pointer-events: auto;
    }

    .mobile-eyebrow {
      font-size: var(--text-xs);
      font-weight: 700;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--color-primary);
      margin-bottom: 1.25rem;
    }

    .mobile-list {
      list-style: none;
      margin: 0;
      padding: 0;
      flex: 1;
      overflow-y: auto;
      -webkit-overflow-scrolling: touch;
    }

    .mobile-link {
      display: flex;
      align-items: center;
      justify-content: space-between;
      width: 100%;
      /* Trailing room so the chevron never sits on the panel edge. */
      gap: 16px;
      padding: 1.15rem 4px 1.15rem 0;
      border: none;
      border-bottom: 1px solid var(--border);
      background: none;
      cursor: pointer;
      text-align: left;
      font-family: var(--font-heading);
      text-transform: uppercase;
      letter-spacing: -0.01em;
      font-size: clamp(1.9rem, 5.5vw, 2.4rem);
      color: var(--color-dark);
    }

    .mobile-chev {
      flex-shrink: 0;
      width: 12px;
      height: 12px;
      margin-right: 4px;
      border-right: 2.5px solid var(--color-primary);
      border-bottom: 2.5px solid var(--color-primary);
      transform: rotate(45deg) translateY(-3px);
      transition: transform var(--dur-fast) ease;
    }

    .mobile-group.open .mobile-chev {
      transform: rotate(-135deg) translateY(-2px);
    }

    .mobile-submenu {
      max-height: 0;
      overflow: hidden;
      transition: max-height var(--dur-base) var(--ease-out-crit);
    }

    .mobile-group.open .mobile-submenu {
      max-height: 60rem;
    }

    .mobile-subgroup {
      border-top: 1px solid var(--border);
    }

    .mobile-subgroup:first-child {
      border-top: none;
    }

    .mobile-sublabel {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      width: 100%;
      padding: 0.8rem 0;
      margin: 0;
      background: none;
      border: none;
      cursor: pointer;
      text-align: left;
      font-family: var(--font-body);
      font-size: var(--text-sm);
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--text-muted);
      padding-right: 4px;
    }

    .mobile-chev.small {
      width: 9px;
      height: 9px;
      border-width: 2px;
    }

    .mobile-subgroup.open .mobile-chev.small {
      transform: rotate(-135deg) translateY(-2px);
    }

    .mobile-sublist {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      max-height: 0;
      overflow: hidden;
      transition: max-height var(--dur-base) var(--ease-out-crit),
        padding var(--dur-base) var(--ease-out-crit);
    }

    .mobile-subgroup.open .mobile-sublist {
      max-height: 40rem;
      padding: 0 0 0.9rem;
    }

    /* Not inside a collapsible sub-group, so it is always open. */
    .mobile-sublist--flat {
      max-height: none;
      padding: 0.9rem 0;
    }

    /* Pills wrap into a compact block — a 17-item vertical list would make
       this menu endless. */
    .mobile-sublink {
      display: inline-flex;
      align-items: center;
      /* Comfortable finger target on the kiosk. */
      padding: 0.7rem 1.15rem;
      border: 1px solid var(--border-strong);
      border-radius: 999px;
      font-size: var(--text-md);
      line-height: 1.1;
      color: var(--color-dark);
      transition: background-color var(--dur-fast) ease, border-color var(--dur-fast) ease, color var(--dur-fast) ease;
    }

    .mobile-sublink:active,
    .mobile-sublink:hover {
      background: var(--color-primary);
      border-color: var(--color-primary);
      color: var(--color-white);
    }

    .mobile-secondary {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-top: 1.25rem;
    }

    .mobile-secondary-btn {
      flex: 1 1 auto;
      text-align: center;
      padding: 0.65rem 1rem;
      border-radius: 999px;
      background: var(--color-cream);
      font-size: var(--text-xs);
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: var(--text-muted);
    }

    .mobile-ctas {
      display: flex;
      flex-direction: column;
      gap: 0.6rem;
      margin-top: 0.75rem;
    }

    .mobile-cta {
      justify-content: center;
      padding: 15px 20px;
    }
  }

  :global(body.nav-mobile-locked) {
    overflow: hidden;
  }
</style>
